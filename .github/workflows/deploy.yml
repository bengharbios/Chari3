name: 🚀 Build & Deploy to Hostinger

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    name: Build Next.js & Deploy
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      # 1. Checkout code
      - name: ⬇️ Checkout repository
        uses: actions/checkout@v4

      # 2. Setup Node.js v20 (matches Hostinger server)
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # 3. Create .env file — dummy values for build only
      #    ✅ Real env vars are already set in Hostinger Dashboard
      #    ✅ prisma generate needs DATABASE_URL format only (no real connection)
      #    ✅ next build doesn't connect to DB (all routes are force-dynamic)
      - name: 🔐 Create .env file (build-time dummy)
        run: |
          echo 'DATABASE_URL="mysql://build_user:build_pass@localhost:3306/build_db"' >> .env
          echo 'NEXTAUTH_SECRET="build-secret-placeholder"' >> .env
          echo 'NEXTAUTH_URL="https://chariday.com"' >> .env
          echo 'BETTER_AUTH_SECRET="build-secret-placeholder"' >> .env
          echo 'BETTER_AUTH_URL="https://chariday.com"' >> .env

      # 4. Install dependencies
      - name: 📦 Install dependencies
        run: npm ci --prefer-offline

      # 5. Build Next.js (prisma generate + next build)
      #    No real DB connection needed — Hostinger has the real .env
      - name: 🔨 Build project
        run: npm run build
        env:
          NODE_ENV: production

      # 6. Create tar archive of .next + public folders
      - name: 📦 Create archive
        run: tar -czf next_build.tar.gz .next public

      # 7. Upload archive to Hostinger via SCP
      - name: 📤 Upload build to Hostinger
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 82.198.227.200
          port: 65002
          username: u584311043
          password: ${{ secrets.HOSTINGER_SSH_PASS }}
          source: "next_build.tar.gz"
          target: "/home/u584311043/domains/chariday.com/nodejs/"

      # 8. Extract archive and restart Passenger
      - name: 🔄 Extract & Restart Passenger
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 82.198.227.200
          port: 65002
          username: u584311043
          password: ${{ secrets.HOSTINGER_SSH_PASS }}
          script: |
            cd /home/u584311043/domains/chariday.com/nodejs
            tar -xzf next_build.tar.gz
            rm -f next_build.tar.gz
            mkdir -p tmp
            touch tmp/restart.txt
            echo "✅ Deployment complete at $(date)"
